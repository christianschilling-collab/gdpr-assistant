rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user has HelloFresh email
    function isHelloFreshUser() {
      return request.auth != null 
        && request.auth.token.email.matches('.*@hellofresh\\.com$');
    }
    
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // ============================================
    // SOLVER RUNS (other team's project)
    // ============================================
    
    // Solver runs -- metadata + adjustments
    // Authenticated HelloFresh users can read and update adjustments.
    // Full result data lives in Firebase Storage (not Firestore).
    match /solverRuns/{runId} {
      allow read, write: if isHelloFreshUser();
    }
    
    // ============================================
    // GDPR ASSISTANT - REPORTING Collections
    // Require Authentication for Read & Write
    // ============================================
    
    // Weekly reports - GDPR compliance reporting
    match /weeklyReports/{reportId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Activity log - detailed activity tracking
    match /activityLog/{logId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Training cases - agent training tracking
    match /trainingCases/{caseId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Market deep dive - detailed market analysis
    match /marketDeepDive/{diveId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Market pulse - quick market updates
    match /marketPulse/{pulseId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // ============================================
    // GDPR ASSISTANT - Other Collections
    // Require Authentication
    // ============================================
    
    // Cases collection
    match /cases/{caseId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Templates collection
    match /templates/{templateId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Template versions
    match /templateVersions/{versionId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Categories collection
    match /categories/{categoryId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Requester types collection
    match /requesterTypes/{typeId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Training collections
    match /trainingRequests/{requestId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    match /trainingCategories/{categoryId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    match /trainingTemplates/{templateId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    match /agentProgress/{progressId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    match /agentPerformance/{performanceId} {
      allow read: if isAuthenticated();
      allow create, update, delete: if isAuthenticated();
    }
    
    // Users collection - restrict to own profile
    match /users/{userId} {
      allow read: if isAuthenticated() && request.auth.token.email == userId;
      allow create, update, delete: if isAuthenticated();
    }
    
    // ============================================
    // QUIZ/GAME Collections
    // ============================================
    
    // Quiz collection
    match /quizzes/{quizId} {
      allow get: if true;  // Anyone can view a quiz
      allow list: if isAuthenticated() 
                  && (resource.data.createdBy == request.auth.uid 
                      || resource.data.privacy == 'public');
      allow create: if isAuthenticated() 
                    && request.resource.data.createdBy == request.auth.uid
                    && request.resource.data.privacy in ['public', 'private'];
      allow update: if isAuthenticated() && (
        resource.data.createdBy == request.auth.uid ||
        (resource.data.privacy == 'public' && request.resource.data.privacy == 'public')
      );
      allow delete: if isAuthenticated() 
                    && resource.data.createdBy == request.auth.uid;
    }
    
    // Game sessions
    match /games/{gameId} {
      allow read: if true;
      allow create: if isAuthenticated();
      allow update: if true;
      allow delete: if isAuthenticated();
    }
  }
}
